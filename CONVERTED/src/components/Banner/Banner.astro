// Import for Markdown files
import { glob } from 'astro';

// Define props (With TypeScript)
interface BannerProps {
  className?: string;
  backgroundColor?: string;
  partNumber: number; 
  lang?: string; 
}

const Banner = async ({ className = "", backgroundColor, partNumber, lang = 'en' }: BannerProps) => {
  // Fetch Markdown file
  const partFiles = await glob(`./part-${partNumber}*.${lang}.md`);
  const partContent = await partFiles[0].then((part) => part.content);

  const background = backgroundColor ? { backgroundColor: backgroundColor } : null;

  return (
    <div className={`banner ${className}`} style={background}>
      <img src={partContent.mainImage} alt="Main Part Image" /> 
      <p>{partContent.intro}</p> 
    </div>
  );
};
<style>
  /* Replicate your CSS from the Banner.scss file */
  .banner {
    display: flex;
    width: 100%;
    align-items: center;
    position: relative;
    /* Assuming you have a prop for padding */
    padding: var(--vertical-spacing) * 2 0;
    /* Assuming you have a prop for background color */
    background-color: var(--main-color);
  }

  @media (min-width: var(--desktop-breakpoint)) { /* Adjust desktop breakpoint as needed */
    .banner {
      padding: 3.444rem; 
      padding-bottom: 4.444rem;
    }
  }
</style>